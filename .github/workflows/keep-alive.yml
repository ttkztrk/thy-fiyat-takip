name: Keep Telegram Bot Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bot health endpoint
        run: curl -sf https://SENIN-BOT-RENDER-URLIN.onrender.com/ || echo "Ping basarisiz, servis yeniden basliyor olabilir"
